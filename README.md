# 🚀 IlyaAhmadi Tunnel
Reverse TCP Tunnel + Port Auto-Sync + Multi-Profile Manager  
📢 Telegram: @ilyaahmadiii

---

## 📌 معرفی
این پروژه یک تونل TCP معکوس (Reverse Tunnel) است که به شما اجازه می‌دهد پورت‌های سرور خارج (EU) را روی سرور ایران (IR) نمایش دهید.

به زبان ساده:
- روی سرور خارج سرویس‌های شما (مثل xray یا هر سرویس TCP) فعال هستند.
- سرور خارج چندین اتصال آماده به سرور ایران می‌سازد (Connection Pool).
- سرور ایران پورت‌ها را باز می‌کند و ترافیک کاربران را از طریق تونل به خارج منتقل می‌کند.

---

## ✨ قابلیت‌ها
✅ Reverse TCP Tunnel  
✅ Auto-Sync پورت‌ها از خارج به ایران  
✅ Connection Pool برای افزایش ظرفیت و سرعت اتصال  
✅ Multi Profile (Iran1..Iran10 / EU1..EU10)  
✅ ساخت و اجرای خودکار systemd service  
✅ نصب و Build خودکار (Go + build)  
✅ مناسب برای مولتی سرور کردن تونل  

---

## ⚡ نصب سریع (One Command)
روی هر دو سرور (ایران و خارج) فقط همین دستور را اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/Zehnovik/ilyaahmadi-tunnel/main/ilyaahmadi_tunnel_manager.sh)



🟢 راه‌اندازی تونل (قدم به قدم)
1) اول روی سرور ایران (IR)
اسکریپت را اجرا کنید:
sudo ./ilyaahmadi_tunnel_manager.sh

سپس:


گزینه 3 را بزنید (Create/Update Iran profile)


یک اسلات انتخاب کنید (مثلاً 1 → iran1)


پورت‌ها را مشخص کنید:


Bridge Port: 7000


Sync Port: 7001




Auto-Sync را y بزنید


در پایان سرویس ساخته و اجرا می‌شود:
ilyaahmadi-iran1.service

چک کردن سرویس:
systemctl status ilyaahmadi-iran1 --no-pager


2) بعد روی سرور خارج (EU)
اسکریپت را اجرا کنید:
sudo ./ilyaahmadi_tunnel_manager.sh

سپس:


گزینه 4 را بزنید (Create/Update EU profile)


یک اسلات انتخاب کنید (مثلاً 1 → eu1)


تنظیمات را وارد کنید:


Iran IP: IP_SERVER_IRAN


Bridge Port: 7000


Sync Port: 7001




در پایان سرویس ساخته و اجرا می‌شود:
ilyaahmadi-eu1.service

چک کردن سرویس:
systemctl status ilyaahmadi-eu1 --no-pager


🔥 مولتی سرور کردن (Multi Server)
حالت 1: چند سرور خارج برای افزایش ظرفیت (پیشنهادی)
اگر چند سرور خارج دارید (EU1, EU2, EU3)، کافی است روی همه آن‌ها:


EU profile بسازید


همه به همان bridge/sync ایران وصل شوند


مثلاً:


eu1 → EU Server 1


eu2 → EU Server 2


eu3 → EU Server 3


در این حالت pool بزرگ‌تر می‌شود و ظرفیت و پایداری بالا می‌رود.

حالت 2: تونل جدا برای هر سرور خارج (تفکیک کامل)
اگر می‌خواهید هر EU کاملاً جدا باشد:
روی ایران:


iran1 → Bridge 7000 Sync 7001


iran2 → Bridge 7002 Sync 7003


iran3 → Bridge 7004 Sync 7005


روی خارج:


eu1 → 7000/7001


eu2 → 7002/7003


eu3 → 7004/7005



🧠 Pool Size چیست؟
Pool Size یعنی تعداد اتصال‌های آماده‌ای که سرور خارج همیشه به سرور ایران برقرار نگه می‌دارد.
مثلاً:


pool=800 → 800 اتصال آماده


pool=1500 → 1500 اتصال آماده


عدد پیشنهادی برای سرورهای معمولی (2core/4GB):


1500



🛠 مدیریت سرویس‌ها
لاگ زنده:
journalctl -u ilyaahmadi-iran1 -f
journalctl -u ilyaahmadi-eu1 -f

ری‌استارت:
systemctl restart ilyaahmadi-iran1
systemctl restart ilyaahmadi-eu1

توقف:
systemctl stop ilyaahmadi-iran1
systemctl stop ilyaahmadi-eu1


⚠️ نکات مهم


همیشه اول ایران را راه‌اندازی کنید، سپس خارج را.


اگر پورت‌ها روی ایران باز نباشند، اتصال برقرار نمی‌شود.


اگر سرویس‌هایی مثل xray روی پورت خاصی listen باشند، آن پورت در sync فعال می‌شود.


اگر پورت خاصی را نمی‌خواهید، باید آن را از سرویس خارج حذف کنید.



📌 Support
اگر مشکل یا سوالی داشتید:
📢 Telegram: @ilyaahmadiii

---

اگر خواستی، من می‌تونم یک نسخه‌ی README خیلی شیک‌تر هم بسازم با:
- جدول تنظیمات
- مثال‌های آماده
- بخش Troubleshooting (ارورهای رایج مثل “file name too long”، “connection refused”، “pool empty” و …)
